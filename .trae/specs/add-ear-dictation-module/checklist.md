- [ ] `Level` 类型支持 `ear_dictation`，并包含听写关卡所需的配置字段（音池、轮数、回放次数、失败回退）。
- [ ] `src/data/levels.ts` 在每个 `song` 关卡前插入听写关卡，且 ID / 回退引用无错乱。
- [ ] `LevelPlay.tsx` 能播放目标音（或短序列）并在不显示答案的前提下让用户作答。
- [ ] 回放次数限制生效，回放用尽后按钮置灰且提示明确。
- [ ] 错误达到上限时自动回退到指定练习关卡，不需要用户手动点击。
- [ ] 听写通过后能自动进入下一关（曲目关卡）。
- [ ] `LevelCard.tsx` 对听写关卡显示“听写”标签且样式一致。
- [ ] `npm run build` 通过。

