- [x] 在 `src/store/useStore.ts` 增加了 `baseOctave` 状态并提供了设置方法。
- [x] `src/utils/keyboardMap.ts` 已经重构为相对音符映射，而不是硬编码带有八度的音高。
- [x] 界面可以监听到数字键 1 到 7 并改变当前的 `baseOctave`。
- [x] 虚拟键盘界面上的音名会根据当前的 `baseOctave` 动态变化（例如从 `C4` 变成 `C1`）。
- [x] 当 `baseOctave` 为 1 时，按下键盘的 `A` 会听到真实的 `C1` 声音，而不是原来的 `C4`。
- [x] 原有教学关卡的正确性验证逻辑仍然正常（比如要求弹奏 `C4`，只有当 `baseOctave=4` 且按下 `A` 时才算正确）。
- [x] 在界面上有明显的“当前八度：X（按数字1-7切换）”的提示语。