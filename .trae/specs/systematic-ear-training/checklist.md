- [ ] `Level` 类型支持 `ear_training`，并具备必要的配置字段（音池、轮数/连对数、回放上限）。
- [ ] 每个 `song` 关卡前都有“听音训练 -> 听写闸门”的前置链路。
- [ ] 听写失败后自动回退到对应的 `ear_training` 关卡，而不是回到键位练习关。
- [ ] 听音训练与听写的 UI 提示清晰：训练态可给提示，考核态隐藏答案且限制回放。
- [ ] `npm run build` 通过。

